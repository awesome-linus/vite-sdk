{
  "name": "vite-sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --mode lcl",
    "build:dev": "tsc && vite build --mode development",
    "build:stg": "tsc && vite build --mode staging",
    "build:prd": "tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "axios": "^1.3.4"
  }
}
